type Customer {
  id: ID
  name: String
  description: String
  email: String
  number: String
  timeZone: String

  org: Org
  phone: PhoneNumber
  mobiePhone: PhoneNumber

  defaultShipTo: Address
  defaultBillTo: Address

  publicListing: Boolean
  moveManaged: Boolean

  contacts: [Contact]
  notes: [Note]
  messages: [Message]
  addresses: [Address]
  customerRelationships: [Customer]
}
